# 协作开发文档

> 本项目基于 ROS 2 Humble + Nav2，仓库巡检路径规划 + 巡逻机器人。多人协作请严格遵守下面的流程，避免把 master 搞乱。

## 一、核心规则（最重要，先记住）

1. **先拷贝（clone）**：拿到最新代码，别直接改别人的工作目录。
2. **必须建新分支**：任何修改都在自己的分支上做。
3. **禁止直接合并到 master**：master 是稳定分支，只能通过 Pull Request（PR）合并。
4. **commit 必须说明修改内容**：每次提交都要写清楚"改了什么、为什么"。

---

## 二、首次获取代码

```bash
git clone https://github.com/mzymzy105/patrol_check_ros2.git
cd patrol_check_ros2
```

如果仓库已经 clone 过，先拉最新代码：

```bash
git checkout master
git pull origin master
```

## 三、环境准备

- Ubuntu 22.04 + ROS 2 Humble + Nav2 + Gazebo
- WSL2 需要设置软件渲染：`export LIBGL_ALWAYS_SOFTWARE=1`
- 详细环境搭建见 `修改说明.md`

---

## 四、标准协作流程（每次改代码都按这个来）

### 第 1 步：从最新的 master 拉一个新分支

```bash
git checkout master
git pull origin master            # 保证基于最新代码
git checkout -b feature/你的功能名   # 新建并切换到新分支
```

### 第 2 步：改代码

在本地改你的文件，改完自测。

### 第 3 步：提交（commit 要说明修改内容）

```bash
git add .
git commit -m "fix: 修复了 xxx 问题

- 具体改了什么
- 为什么这么改"
```

> ⚠️ commit 信息**必须**说清楚修改内容，禁止写 "update"、"改了点东西" 这种没意义的描述。

### 第 4 步：推自己的分支（不要推 master）

```bash
git push origin feature/你的功能名
```

### 第 5 步：提 Pull Request（PR）

在 GitHub 上把 `feature/你的功能名` 发起 PR 合并到 `master`，**由其他人 review 后再合并**，不要自己直接合。

---

## 五、分支命名规范

| 类型 | 命名 | 例子 |
|------|------|------|
| 新功能 | `feature/描述` | `feature/add-elevator-costmap` |
| 修 bug | `fix/描述` | `fix/astar-node-pool` |
| 文档 | `docs/描述` | `docs/readme` |
| 重构 | `refactor/描述` | `refactor/planner` |

## 六、Commit 信息规范

用「类型 + 一句话说明」，第一行简短，下面可加详细说明：

| 前缀 | 用途 | 例子 |
|------|------|------|
| `feat:` | 新功能 | `feat: 增加坡度代价层` |
| `fix:` | 修 bug | `fix: 修复 A* 节点池越界` |
| `docs:` | 文档 | `docs: 补充运行说明` |
| `refactor:` | 重构 | `refactor: 拆分规划器模块` |
| `chore:` | 杂项 | `chore: 更新 .gitignore` |

---

## 七、注意事项

1. **大文件不要提交**：`*.pcd`、`*.log`、`build/`、`install/` 等已加进 `.gitignore`，千万别 `git add -f` 强行提交。
2. **不要提交凭证**：PAT、`.git-credentials` 等绝不能进仓库。
3. **提交前先 `git status`**：确认没误加不该提交的文件。
4. **冲突了先拉最新再解决**：`git pull origin master` 合并到自己的分支，解决冲突后再 push。
5. **不要用 `git push -f`**：强制推送会覆盖别人的提交。

---

## 八、常见操作速查

```bash
# 查看当前分支
git branch

# 切换分支
git checkout 分支名

# 查看修改了哪些文件
git status

# 查看某次提交改了啥
git show 提交哈希

# 撤销本地未提交的改动（慎用）
git checkout -- 文件名

# 拉取最新代码合并到当前分支
git pull origin master
```
