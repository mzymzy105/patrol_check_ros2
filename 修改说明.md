# 修改说明

本次会话（在 WSL2 / Ubuntu 22.04 环境）完成的工作，让「扫地机 + 用户地图 + Nav2 导航」整套流程跑通。

## 一、环境搭建

- 安装 ROS 2 Humble + Nav2 + Gazebo 11 + ros2_control（通过清华镜像，因为官方源在国内访问超时）。
- 解决 WSL2 无 GPU 直通导致的 Gazebo/RViz 段错误崩溃：在 `~/.bashrc` 里加了
  ```bash
  export LIBGL_ALWAYS_SOFTWARE=1
  ```
  强制用 llvmpipe 软件渲染。

## 二、路径修复（去掉硬编码机器路径）

| 文件 | 修改 |
|------|------|
| `user_map.launch.py` | 原来硬编码 `/home/hhl/Desktop/plan/...`，改成用 `os.path.dirname(os.path.abspath(__file__))` 推导，换机器不再报错 |
| `nav2_world/world.world` | `map_walls.stl` 的 mesh 路径从 `/home/hhl/...` 改成当前机器路径 |

## 三、导航修复

### 1. `nav2_demo.py` — 初始位姿发布修复

原代码用 `rclpy.time.Time().to_msg()`（时间戳=0）且只发 1 次。AMCL 的 `/initialpose` 订阅是 **BEST_EFFORT** QoS，单次发布会被丢弃，导致 AMCL 一直收不到位姿、无法定位（日志反复报 `AMCL cannot publish a pose... Please set the initial pose`）。

修复：改成**连发 5 次 + 用当前时间戳** `node.get_clock().now()`。

### 2. 自定义 A* 规划器 bug 修复（嵌套项目 `ros2-patrol-robot-nav2`）

文件：`src/nav2_custom_planner/src/nav2_custom_planner.cpp`

根因：`configure()` 执行时代价地图还是默认尺寸 **100×100**（地图是异步加载的），此时 `node_pool_` 只分配了 10000 个节点；等地图真正加载后代价地图变成 **376×222**，搜索时 `hash = y*size_x + x` 会越界访问，导致 A* 找不到任何路径（报 `A*规划失败：无法找到从起点到目标点的路径`）。

修复：在 `createPlan()` 搜索前，按当前代价地图尺寸重新 `resize` 节点池。

## 四、运行方式

```bash
# 1. 启动 Gazebo（headless）+ 扫地机
source /opt/ros/humble/setup.bash
cd ~/tracecheck/plan_part
ros2 launch user_map.launch.py

# 2. 启动 Nav2 导航栈（另开终端）
source /opt/ros/humble/setup.bash
ros2 launch nav2_bringup bringup_launch.py \
  map:=~/tracecheck/plan_part/nav2_map/map.yaml \
  params_file:=~/tracecheck/plan_part/nav2_map/sweeper.yaml \
  use_sim_time:=true

# 3. 设初始位姿 + 发导航目标
python3 nav2_demo.py <目标x> <目标y>

# 4.（可选）RViz 可视化
ros2 run rviz2 rviz2 -d ~/tracecheck/plan_part/nav2_map/my_map.rviz
```

## 五、待办 / 注意事项

- `sweeper.yaml` 里各节点 `use_sim_time` 写得不一致（有的 True 有的 False），靠 launch 参数 `use_sim_time:=true` 统一覆盖，后续可清理。
- 扫地机默认 spawn 在 `(-19.9, 77.86)`，默认目标 `(3,3)` 约 78 米远，机器人最快 0.22 m/s，跑完全程要几分钟；快速验证可换近一点的目标。
- 原始 LiDAR 数据 `20260804_211333/` 已加入 `.gitignore`（数百 MB），本地保留即可。
